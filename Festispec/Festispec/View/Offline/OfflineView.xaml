<Window x:Class="Festispec.View.OfflineView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Festispec.View"
        mc:Ignorable="d"
        Title="OfflineView" Height="450" Width="800">
    <Grid DataContext="{Binding Offline, Source={StaticResource Locator}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <DataGrid Grid.Row="0" Grid.Column="0" ItemsSource="{Binding Inspections}" SelectedItem="{Binding SelectedInspection, Mode=TwoWay}"/>
        <Button Grid.Row="0" Grid.Column="1" Command="{Binding SaveInspectionCommand}" Width="80" Height="30" Content="Save Inspection"></Button>
        <Button Grid.Row="1" Grid.Column="1" Command="{Binding OpenSavedInspectionsCommand}" Width="80" Height="30" Content="Offline Inspections"></Button>
    </Grid>

    
</Window>
