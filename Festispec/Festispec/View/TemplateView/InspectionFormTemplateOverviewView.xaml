<Page x:Class="Festispec.View.TemplateView.InspectionFormTemplateOverviewView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Festispec.View.TemplateView"
      xmlns:components="clr-namespace:Festispec.View.Components"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InspectieformulierTemplateOverviewView"
      DataContext ="{Binding InspectionFormTemplateOverview, Source={StaticResource Locator}}">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <components:OverviewHeader x:Name="header" PageName="Templates" Grid.Row="0" />
        <components:OverviewSearchBar x:Name="searchBar" Grid.Row="1" ButtonText="Template Aanmaken" ButtonActionCommand="{Binding CreateButtonClickCommand}" SearchActionCommand="{Binding SearchButtonClickCommand}" />

        <ListView Grid.Row="3" ItemsSource="{Binding Templates}" HorizontalAlignment="Stretch" VerticalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionMode="Single">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="20" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="30" FontWeight="Bold" TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" />
                        <TextBlock Grid.Row="1" Text="{Binding Description}" TextWrapping="Wrap" MaxHeight="200" Margin="0, 20" HorizontalAlignment="Center"/>
                        <Button Content="Aanmaken" Padding="5" Command="{Binding DataContext.SelectTemplateCommand, RelativeSource={RelativeSource AncestorType={x:Type local:InspectionFormTemplateOverviewView}}}" CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=DataContext}" Visibility="{Binding DataContext.IsSelectMode, RelativeSource={RelativeSource AncestorType={x:Type local:InspectionFormTemplateOverviewView}}}" />
                        <Button Content="Aanpassen" Padding="5" Width="150" HorizontalAlignment="Center" Command="{Binding DataContext.EditTemplateCommand, RelativeSource={RelativeSource AncestorType={x:Type local:InspectionFormTemplateOverviewView}}}" CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=DataContext}" Visibility="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType={x:Type local:InspectionFormTemplateOverviewView}}}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
